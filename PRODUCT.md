# Product

## Register

product

## Users

CTools 面向在 macOS 上使用 Codex Desktop、且需要在 ChatGPT 登录模式与兼容 OpenAI Responses API 的自定义供应商之间切换的开发者和技术人员。用户通常正在排查 API、验证供应商或恢复工作环境，需要快速完成操作且不能因配置错误失去 Codex 的可用性。

## Product Purpose

CTools 用可逆事务管理 Codex 的运行模式：保存原配置、测试目标供应商、写入受控配置、校验并重启 Codex；任何一步失败都恢复原始配置。成功意味着用户无需手工编辑 `config.toml` 或暴露 API Key，就能可靠切换并随时一键还原。

## Brand Personality

克制、可靠、技术明确。界面应让高风险配置操作显得可理解、可核对、可恢复，避免夸张承诺和娱乐化表达。

## Anti-references

- 不做带大量装饰和营销话术的 SaaS 控制台。
- 不把关键恢复能力藏进二级菜单或模糊图标。
- 不用新奇控件替代熟悉的表单、下拉选择和状态反馈。
- 不通过密集动画、玻璃效果或过度发光削弱配置信息的可读性。

## Design Principles

- 安全路径始终可见：切换、失败恢复和紧急还原都明确显示结果。
- 单一事实来源：共享设置只配置一次，使用处展示当前有效值。
- 先验证再修改：测试与校验发生在配置生效之前。
- 减少易错输入：可选择的值优先使用标准控件，敏感或精确字段提供清晰提示。
- 保留用户控制：CTools 管理切换默认值，不限制 Codex Desktop 中后续可用的选择。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为基线；键盘焦点必须可见，状态不能只依赖颜色表达，正文与表单提示保持足够对比度，并尊重系统的减少动态效果设置。
